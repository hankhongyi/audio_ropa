<Window x:Class="AudioRopa.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioRopa"
        xmlns:pages="clr-namespace:AudioRopa.Pages"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource PrimaryBlackBrush}"
        Title="AudioRopa" Height="720" Width="1380">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" x:Name="BlackCircle" Source="/Images/audio_ropa_logo.png" Margin="20,10,0,0"
                       Width="250" Height="60" HorizontalAlignment="Left" Visibility="Visible" Stretch="Fill"/>
            <TextBlock Grid.Column="1" x:Name="VersionText" Text="0" Margin="0,0,20,0" HorizontalAlignment="Right" Style="{StaticResource NormalTextBlockStyle}" />
        </Grid>
        
        <!-- Main Content -->
        <DockPanel x:Name="MainDockPanel" Grid.Row="1">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Visible"
                         HorizontalScrollBarVisibility="Disabled"
                         Margin="0,0,0,0">
                    <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden">
                        <Frame.Content>
                            <pages:DeviceSelectionPage />
                        </Frame.Content>
                    </Frame>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
